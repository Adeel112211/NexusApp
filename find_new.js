const fs = require('fs'); const path = require('path'); const baseDir = 'd:/Adeel/App/public/Images'; const playData = fs.readFileSync('d:/Adeel/App/src/data/playData.ts', 'utf8'); const categories = fs.readdirSync(baseDir); for (const cat of categories) { if (!fs.statSync(path.join(baseDir, cat)).isDirectory()) continue; const apps = fs.readdirSync(path.join(baseDir, cat)); for (const app of apps) { if (!playData.includes(app.replace(/[^a-zA-Z0-9]/g, '-').toLowerCase())) { console.log('New app found:', cat, '/', app); } } }
